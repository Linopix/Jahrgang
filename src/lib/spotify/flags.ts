/**
 * Spotify Web API (Client Credentials).
 * Off until you set this true AND add SPOTIFY_CLIENT_ID + SPOTIFY_CLIENT_SECRET
 * in the host env (Vercel → Settings → Environment Variables).
 */
export const SPOTIFY_LIVE = false;
